# 发现配置

刻意保持核心精简，但留出了清晰的扩展点：
- `scope` 字段描述为“路径或资源标识符”，已经暗示了未来可支持 S3、FTP 等非本地资源；
- `assign` 是自由字典，可随时增加字段而不破坏兼容性；
- `filters` 目前只有 `excludes`，但结构上完全可加入 `includes` 或其他过滤器；
- 映射规则基于标准 Glob，如果需要内容嗅探，可以通过自定义 `match` 函数扩展（例如约定 match: "magic:PDF" 的语法）。
